---
title: Batch reads through one eth_call
description: Batch contract reads through one CREATE-style eth_call.
template: splash
hero:
  title: Batch reads through one eth_call
  tagline: ghostcall combines contract reads into one eth_call. No contract deployment or onchain dependencies are required.
head:
  - tag: title
    content: ghostcall — Batch reads through one eth_call
  - tag: meta
    attrs:
      property: og:title
      content: ghostcall — Batch reads through one eth_call
  - tag: meta
    attrs:
      name: twitter:title
      content: ghostcall — Batch reads through one eth_call
---

import ApiIndex from "../../components/ApiIndex.astro";
import BatchFlow from "../../components/BatchFlow.astro";
import ManualSection from "../../components/ManualSection.astro";
import PayloadFigure from "../../components/PayloadFigure.astro";

<BatchFlow />

<ManualSection index="01" title="Install" id="install">

```sh title="Terminal"
npm install @volga-sh/evm-ghostcall
```

</ManualSection>

<ManualSection index="02" title="Read two values" id="read-two-values">

This example reads the WETH total supply and one account balance on Ethereum.
ghostcall sends both reads in one RPC request and returns decoded `bigint`
values in the same order.

```ts title="Listing 2.1 · TypeScript"
import { aggregateDecodedCalls } from "@volga-sh/evm-ghostcall";
import { balanceOf, totalSupply } from "./erc20";

const token = "0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2";
const account = "0x28C6c06298d514Db089934071355E5743bf21d60";
const balanceCall = balanceOf(account);

const [supply, balance] = await aggregateDecodedCalls(provider, [
	{
		to: token,
		data: totalSupply.data,
		decodeResult: totalSupply.decodeReturn,
	},
	{
		to: token,
		data: balanceCall.data,
		decodeResult: balanceCall.decodeReturn,
	},
]);
```

</ManualSection>

<ManualSection index="03" title="How it works" id="how-it-works">

Batching reads normally means deploying a Multicall contract and routing every
read through it. ghostcall removes that dependency by using a step the EVM
already performs: contract creation.

An `eth_call` without a `to` address makes the EVM treat its `data` as initcode,
the code that runs once when a contract is created. Initcode normally ends by
returning the runtime bytecode to store onchain. ghostcall returns batch results
instead. Its 91-byte program reads the call entries appended after its own bytes,
runs each one in order, and returns a packed result entry per call.

Because `eth_call` only simulates execution, no contract is deployed and no state
change is saved.

<PayloadFigure />

<p class="manual-follow-link not-content">
	<a href="/protocol/">Protocol <span aria-hidden="true">→</span></a>
	<a href="/limits/">Limits <span aria-hidden="true">→</span></a>
</p>

</ManualSection>

<ManualSection index="04" title="API index" id="api-index">

<ApiIndex />

<p class="manual-follow-link not-content">
	<a href="/api/">Complete API reference <span aria-hidden="true">→</span></a>
</p>

</ManualSection>
